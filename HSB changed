function setup() {
  createCanvas(400, 400);
  background(220,5,20);
  // HSB with H range 0-400, S range 0-400, B range 0-100:
  colorMode(HSB, 20, 350, 20);

}
function draw() {
  var x = random(width)
  var y = random(height);
  
  fill(x, y, 100);
  strokeWeight(2)
  ellipse(x, y, 20);
 
}
